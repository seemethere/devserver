---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: devservers.apps.devservers.io
spec:
  group: apps.devservers.io
  names:
    kind: DevServer
    listKind: DevServerList
    plural: devservers
    singular: devserver
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DevServer is the Schema for the devservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DevServer
            properties:
              distributed:
                description: Distributed configuration for distributed training mode
                properties:
                  backend:
                    default: nccl
                    description: Backend specifies the distributed backend (nccl,
                      gloo, mpi)
                    enum:
                    - nccl
                    - gloo
                    - mpi
                    type: string
                  ncclSettings:
                    additionalProperties:
                      type: string
                    description: NCCLSettings provides NCCL-specific configuration
                    type: object
                  nprocsPerNode:
                    default: 1
                    description: NProcsPerNode specifies the number of processes per
                      node
                    format: int32
                    minimum: 1
                    type: integer
                  worldSize:
                    description: WorldSize specifies the total number of processes
                      in distributed training
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - worldSize
                type: object
              enableSSH:
                default: true
                description: EnableSSH enables SSH access to the development server
                type: boolean
              flavor:
                description: Flavor references a DevServerFlavor resource that defines
                  compute resources
                type: string
              image:
                default: company/pytorch-dev:latest
                description: Image specifies the container image to use for the development
                  server
                type: string
              lifecycle:
                description: Lifecycle defines lifecycle management settings
                properties:
                  autoShutdown:
                    default: true
                    description: AutoShutdown enables automatic shutdown when idle
                      timeout is reached
                    type: boolean
                  idleTimeout:
                    default: 3600
                    description: IdleTimeout specifies how long (in seconds) the server
                      can be idle before auto-shutdown
                    format: int32
                    minimum: 60
                    type: integer
                type: object
              mode:
                default: standalone
                description: Mode specifies whether this is a standalone server or
                  distributed training
                enum:
                - standalone
                - distributed
                type: string
              owner:
                description: Owner specifies the email of the user who owns this DevServer
                type: string
              persistentHomeSize:
                anyOf:
                - type: integer
                - type: string
                default: 100Gi
                description: PersistentHomeSize specifies the size of the persistent
                  home directory volume
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              sharedVolumeClaimName:
                description: SharedVolumeClaimName specifies the name of the shared
                  EFS volume claim
                type: string
            required:
            - flavor
            - owner
            type: object
          status:
            description: status defines the observed state of DevServer
            properties:
              conditions:
                description: conditions represent the current state of the DevServer
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastIdleTime:
                description: LastIdleTime records the last time the server was detected
                  as idle
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the DevServer lifecycle
                enum:
                - Pending
                - Running
                - Terminating
                - Failed
                type: string
              podNames:
                description: PodNames lists the names of pods created for this DevServer
                items:
                  type: string
                type: array
              ready:
                description: Ready indicates whether the DevServer is ready for use
                type: boolean
              serviceName:
                description: ServiceName is the name of the service created for this
                  DevServer
                type: string
              sshEndpoint:
                description: SSHEndpoint provides the SSH connection information when
                  SSH is enabled
                type: string
              startTime:
                description: StartTime records when the DevServer was started
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
